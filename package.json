{
    "name": "dms-keystrokes",
    "version": "1.3.0",
    "private": true,
    "description": "Keystroke and mouse overlay for DankMaterialShell, ported from keyviz",
    "scripts": {
        "test": "node --test tests/*.test.cjs"
    }
}
